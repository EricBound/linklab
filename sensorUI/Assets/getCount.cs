using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class getCount : MonoBehaviour
{
    public int lol;
    // Start is called before the first frame update
    void Start()
    {
        lol = instantiate.count;
        Debug.Log(lol);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
